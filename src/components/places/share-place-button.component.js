import { View, Text } from "react-native";
import React from "react";

const SharePlaceButton = ({ place }) => {
  return (
    <View>
      <Text>SharPlaceButton</Text>
    </View>
  );
};

export default SharePlaceButton;
